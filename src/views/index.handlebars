{{> carousel}}
{{!<div class="panel"></div>}}
<div class="container">
    <div class="row">
        <div class="col-md-6">
            {{> email_collection}}
        </div>
        <div class="col-md-6">
            {{> email_collection}}
        </div>
    </div>
</div>